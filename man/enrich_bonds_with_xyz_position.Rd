% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{enrich_bonds_with_xyz_position}
\alias{enrich_bonds_with_xyz_position}
\title{Enrich bond data with atom coordinates}
\usage{
enrich_bonds_with_xyz_position(
  bonds,
  atoms,
  origin = "origin",
  target = "target",
  atom_id = "eleno"
)
}
\arguments{
\item{bonds}{Data frame of bonds containing columns that reference atom IDs.}

\item{atoms}{Data frame of atoms containing coordinates \code{x}, \code{y}, \code{z} and an
atom identifier column.}

\item{origin}{Name of the column in \code{bonds} giving the origin atom ID.}

\item{target}{Name of the column in \code{bonds} giving the target atom ID.}

\item{atom_id}{Name of the column in \code{atoms} giving the atom ID used to match
against \code{origin} and \code{target}.}
}
\value{
A data frame identical to \code{bonds} but with six additional coordinate
columns: \code{x}, \code{y}, \code{z} (for origin atoms) and \code{xend}, \code{yend}, \code{zend} (for
target atoms).
}
\description{
Joins atomic coordinate columns (\code{x}, \code{y}, \code{z}) to a bond table so that each
bond row includes both start (\code{x}, \code{y}, \code{z}) and end (\code{xend}, \code{yend}, \code{zend})
positions corresponding to its origin and target atoms.
}
\examples{
atoms <- data.frame(
  eleno = 1:2,
  x = c(0, 1), y = c(0, 1), z = c(0, 1)
)
bonds <- data.frame(origin = 1, target = 2)
enrich_bonds_with_xyz_position(bonds, atoms)

}
